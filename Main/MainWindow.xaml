<Window x:Class="Main.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cqlWpfTxt="clr-namespace:MainCore.CQL.WPF.Textual;assembly=MainCore.CQL.WPF"
        xmlns:cqlWpfCmp="clr-namespace:MainCore.CQL.WPF.Composer;assembly=MainCore.CQL.WPF"
        xmlns:editor="clr-namespace:MainCore.CQL.WPF;assembly=MainCore.CQL.WPF"
        xmlns:local="clr-namespace:Main"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="600">
    <Grid Margin="0,0,0,0">
        <editor:Editor Margin="10,10,10,0" Height="28" VerticalAlignment="Top" Context="{Binding Context}" Query="{Binding Query,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
        <ListView x:Name="listView" Margin="10,43,10,10" ItemsSource="{Binding FilteredSubjects}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Age" DisplayMemberBinding="{Binding Age}"/>
                    <GridViewColumn Header="Class" DisplayMemberBinding="{Binding Class}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
